* Convert primitives to have a single return point, to facilitate inlining and different execution models
* Split macros and primitives into separate compilation source files
* Write a test-harness for regression testing
* Conform to C calling conventions?
* Work out what to do with prims that jmp rather than ret
* Cache label in MMX register (also allows trivial 64bit comparison)
* Add stack-effects and side-effect warning to dictionary headers
* test: 1 10 < if 0xff .h else 0 .h endif ;  -- fails with "Not a dictionary word or number: 0" but test: 0 .h ; succeeds
